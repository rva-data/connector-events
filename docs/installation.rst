============
Installation
============

At the command line::

    $ easy_install connector-events

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv connector-events
    $ pip install connector-events